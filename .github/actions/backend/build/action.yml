name: "Backend Build"
description: "Build the backend"
runs:
  using: composite
  steps:
    - uses: actions/checkout@v4
    - name: Set up pnpm
      uses: pnpm/action-setup@v4
      with:
        version: 9.12.0
    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '22.x'
        cache: 'pnpm'
    - name: Build backend
      shell: bash
      run: |
        pnpm --config.node-linker=hoisted --filter backend deploy --prod apps/throwtrash-web/backend/src/backend-dist
    - name: Prepare backend layer
      shell: bash
      run: |
        set -euo pipefail
        # pnpm の symlink を避けるため hoisted の node_modules をレイヤーに移す
        layer_root="apps/throwtrash-web/backend/src/layer/nodejs"
        rm -rf "${layer_root}"
        mkdir -p "${layer_root}"
        mv "apps/throwtrash-web/backend/src/backend-dist/node_modules" "${layer_root}/node_modules"
    - name: Archive backend
      uses: actions/upload-artifact@v4
      with:
        name: backend
        path: apps/throwtrash-web/backend/src/backend-dist
        retention-days: 1
    - name: Archive backend layer
      uses: actions/upload-artifact@v4
      with:
        name: backend-layer
        path: apps/throwtrash-web/backend/src/layer
        retention-days: 1
