name: "Backend Build"
description: "Build the backend"
runs:
  using: composite
  steps:
    - uses: actions/checkout@v4
    - name: Set up pnpm
      uses: pnpm/action-setup@v4
      with:
        version: 9.12.0
    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '22.x'
        cache: 'pnpm'
    - name: Install dependencies
      shell: bash
      run: pnpm install --frozen-lockfile
    - name: Build backend
      shell: bash
      run: pnpm --filter backend run build
    - name: Prepare layer directory
      shell: bash
      run: |
        layer_root="apps/throwtrash-web/backend/src/layer/nodejs"
        rm -rf "${layer_root}/node_modules"
        mkdir -p "${layer_root}/node_modules"
        rsync -aL apps/throwtrash-web/backend/src/node_modules/ "${layer_root}/node_modules/"
    - name: Archive backend
      uses: actions/upload-artifact@v4
      with:
        name: backend
        path: apps/throwtrash-web/backend/src/backend-dist
        retention-days: 1
    - name: Archive backend layer
      uses: actions/upload-artifact@v4
      with:
        name: backend-layer
        path: apps/throwtrash-web/backend/src/layer
        retention-days: 1
